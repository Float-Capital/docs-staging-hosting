name: Run Contract Tests

on: push

jobs:
  # Source: https://github.community/t/run-job-only-if-folder-changed/118292/2
  has-contract-changes:
    name: Check files
    outputs:
      run_job: ${{ steps.check_files.outputs.run_job }}
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v2
        with:
          fetch-depth: 2

      - name: check modified files
        id: check_files
        run: |
          echo "=============== list modified files ==============="
          git diff --name-only HEAD^ HEAD

          echo "========== check paths of modified files =========="
          git diff --name-only HEAD^ HEAD > files.txt
          while IFS= read -r file
          do
            echo $file
            if [[ $file != contracts/* ]]; then
              echo "This modified file is not under the 'contracts' folder."
              echo "::set-output name=run_job::false"
              break
            else
              echo "::set-output name=run_job::true"
            fi
          done < files.txt

  push-ui-for-vercel:
    name: Run contract tests
    runs-on: ubuntu-latest
    if: needs.has-contract-changes.outputs.run_job == 'true'

    steps:
      # Checks out a copy of your repository on the ubuntu-latest machine
      - name: Checkout code
        uses: actions/checkout@v2

      - name: install node packages
        id: install_node_packages
        run: |
          cd contracts && yarn

      - name: run contract tests
        id: run_contract_tests
        run: |
          cd contracts && yarn test
