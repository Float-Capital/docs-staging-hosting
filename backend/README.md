# How to use:

Install dependencies:

```bash
npm i -g hasura-cli
```

```bash
cd hasura-actions
yarn
```

Create the env file:

```bash
cp .example.env .env
```

Start the backend:

```bash
make hard-restart-dev
```
