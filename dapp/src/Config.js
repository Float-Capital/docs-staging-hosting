// Generated by ReScript, PLEASE EDIT WITH CARE

import * as Js_exn from "bs-platform/lib/es6/js_exn.js";
import * as Belt_Option from "bs-platform/lib/es6/belt_Option.js";

var defaultConfig = "./config.json";

var getConfig = ((configLocation) => require(configLocation));

var configPath = Belt_Option.getWithDefault(process.env.CONFIG_FILE, defaultConfig);

var config = Belt_Option.getWithDefault(getConfig(configPath), Js_exn.raiseError("Could not find config file at " + configPath));

var match = config.contracts;

var graphEndpoint = config.graphEndpoint;

var networkId = config.networkId;

var networkName = config.networkName;

var paymentTokenName = config.paymentTokenName;

var blockExplorer = config.blockExplorer;

var blockExplorerName = config.blockExplorerName;

var discordInviteLink = config.discordInviteLink;

var longShort = match.LongShort;

var staker = match.Staker;

var dai = match.Dai;

var floatToken = match.FloatToken;

export {
  defaultConfig ,
  getConfig ,
  configPath ,
  config ,
  graphEndpoint ,
  networkId ,
  networkName ,
  paymentTokenName ,
  blockExplorer ,
  blockExplorerName ,
  discordInviteLink ,
  longShort ,
  staker ,
  dai ,
  floatToken ,
  
}
/* configPath Not a pure module */
