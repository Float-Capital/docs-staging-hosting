// Generated by ReScript, PLEASE EDIT WITH CARE
'use strict';

var Curry = require("rescript/lib/js/curry.js");
var React = require("react");

function useKeyPress(targetKey) {
  var match = React.useState(function () {
        return false;
      });
  var setKeyPressed = match[1];
  var downHandler = function (keyboardEvent) {
    if (keyboardEvent.key === targetKey) {
      return Curry._1(setKeyPressed, (function (param) {
                    return true;
                  }));
    }
    
  };
  var upHandler = function (keyboardEvent) {
    if (keyboardEvent.key === targetKey) {
      return Curry._1(setKeyPressed, (function (param) {
                    return false;
                  }));
    }
    
  };
  var preventArrowScrolling = function (keyboardEvent) {
    if ([
          "ArrowUp",
          "ArrowDown"
        ].indexOf(keyboardEvent.key) > -1) {
      keyboardEvent.preventDefault();
      return ;
    }
    
  };
  React.useEffect((function () {
          window.addEventListener("keydown", downHandler);
          window.addEventListener("keyup", upHandler);
          window.addEventListener("keydown", preventArrowScrolling);
          return (function (param) {
                    window.removeEventListener("keydown", downHandler);
                    window.removeEventListener("keyup", upHandler);
                    window.removeEventListener("keydown", preventArrowScrolling);
                    
                  });
        }), []);
  return match[0];
}

exports.useKeyPress = useKeyPress;
/* react Not a pure module */
