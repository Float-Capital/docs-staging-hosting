// Generated by ReScript, PLEASE EDIT WITH CARE

import * as React from "react";
import * as Js_math from "bs-platform/lib/es6/js_math.js";
import * as Belt_Array from "bs-platform/lib/es6/belt_Array.js";

function formatMoney(number) {
  var decimals = String(Math.abs((number * 100 | 0) % 100));
  var removeCommas = function (str) {
    return str.replace(/[,]/g, "");
  };
  var numberWithoutDecimals = Js_math.floor_int(number);
  var arr = String(numberWithoutDecimals).split("");
  var reversedSeperatedNumber = removeCommas(Belt_Array.reverse(arr).toString()).match(/.{1,3}/g);
  var formattedMoney;
  if (reversedSeperatedNumber !== null) {
    var arr$1 = Belt_Array.map(reversedSeperatedNumber, (function (part) {
            var arr = part.split("");
            return removeCommas(Belt_Array.reverse(arr).toString());
          }));
    formattedMoney = Belt_Array.reverse(arr$1).toString();
  } else {
    formattedMoney = "";
  }
  return formattedMoney + "." + decimals;
}

function FormatMoney(Props) {
  var number = Props.number;
  var formattedMoney = formatMoney(number);
  return React.createElement("span", undefined, formattedMoney);
}

var make = FormatMoney;

export {
  formatMoney ,
  make ,
  
}
/* react Not a pure module */
